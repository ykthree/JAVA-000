server:
  port: 8082
  servlet:
    context-path: /
  address: 0.0.0.0
spring:
  datasource:
    driver-class-name:  com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/dollar_account?useSSL=false&useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC
    username: root
    password:

logging:
  level:
    root: error
    org.springframework.web: info
    org.apache.ibatis: debug
    learn.tcc.exchange: debug
